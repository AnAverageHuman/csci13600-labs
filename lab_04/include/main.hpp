#ifndef MAIN_HPP
#define MAIN_HPP

#include <string>

const char MUH_CHAR = '*';
const char MUH_SPACE = ' ';

std::string box(int width, int height);
std::string checkerboard(int width, int height);

#endif // MAIN_HPP

